spring:
  messages:
    basename: messages
  jpa:
    show-sql: true
    generate-ddl: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;
    driver-class-name: org.h2.Driver
    data-username: sa
    data-password:

